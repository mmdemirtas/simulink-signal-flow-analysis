The simulation is designed to analyze how an input signal propagates through a multi-stage system, including filtering, modulation/transfer behavior.
Several scope blocks are used to visualize system responses at different nodes, allowing comparison between intermediate and final outputs.

How to Run
Open the .slx file in MATLAB Simulink
Click Run to start the simulation
Observe outputs in Scope blocks
Compare intermediate vs final system response
